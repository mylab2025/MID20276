<Window x:Class="MyLab_V2_WPF.BomWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MyLab - BOM Manager" Height="710" Width="1120"
        WindowStartupLocation="CenterScreen"
        Background="#ecf0f1">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <DockPanel Grid.Row="0" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left">
                <TextBlock Text="Bill of Materials"
                   FontSize="22" FontWeight="Bold"
                   Foreground="#2d3436" VerticalAlignment="Center"/>
                
            </StackPanel>
            
            <CheckBox x:Name="AdditionalInfoCheckBox"
              Content="Show Additional Info"
              DockPanel.Dock="Right" Margin="10,0,0,0"
              VerticalAlignment="Center" HorizontalAlignment="Right"
              Checked="AdditionalInfoCheckBox_Changed"
              Unchecked="AdditionalInfoCheckBox_Changed"/>
            <CheckBox x:Name="EditModeCheckBox"
  Content="Edit Mode"
  Margin="20,0,0,0"
  VerticalAlignment="Center"
  Checked="EditModeCheckBox_Checked"
  HorizontalAlignment="Right"
  Unchecked="EditModeCheckBox_Checked"/>
        </DockPanel>


        <!-- BOM Grid Area -->
        <Border Grid.Row="1"
                Background="White" CornerRadius="8"
                Padding="5" BorderBrush="#dfe6e9" BorderThickness="1">
            <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <Grid x:Name="BomGrid">
                    <!-- Dynamic content loaded from code-behind -->
                </Grid>
            </ScrollViewer>
        </Border>

        <!-- Footer Section -->
        <StackPanel Grid.Row="2" Margin="0,15,0,0">
            <TextBlock x:Name="TotalCostTextBlock"
                       FontWeight="Bold" FontSize="16"
                       Foreground="#2c3e50"
                       HorizontalAlignment="Right" Margin="0,0,10,5"/>

            


            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="+ Add Row"
                        Width="100" Margin="5" Click="AddRow_Click"
                        FontWeight="Bold" Background="#74b9ff"/>
                <Button Content="Export PDF"
                        Width="100" Margin="5" Click="ExportPdf_Click"
                        FontWeight="Bold" Background="#55efc4"/>
                <Button Content="Export Excel"
                        Width="110" Margin="5" Click="ExportExcel_Click"
                        FontWeight="Bold" Background="#81ecec"/>
                <Button Content="Clear"
                        Width="90" Margin="5" Click="Clear_Click"
                        FontWeight="Bold" Background="#fab1a0"/>
                <Button x:Name="SaveChangesButton"
                        Content="Save Changes"
                        Width="120" Margin="5"
                        Click="SaveChangesButton_Click"
                        FontWeight="Bold"
                        Background="#ffeaa7"
                        Visibility="Collapsed"/>
            </StackPanel>
            <ProgressBar x:Name="SaveProgressBar"
             Minimum="0" Maximum="100"
             Height="20" Margin="0,10,0,0"
             Visibility="Collapsed"
             Foreground="Green"/>            

        </StackPanel>
    </Grid>
</Window>
